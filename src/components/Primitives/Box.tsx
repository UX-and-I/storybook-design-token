import styled from '@emotion/styled';

export const Box = styled.div(() => ({
  width: '140px',
  height: '25px',
  borderRadius: '4px',
  backgroundColor: '#fff',
  position: 'relative',
  zIndex: 2
}));
